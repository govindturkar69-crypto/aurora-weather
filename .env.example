# Aurora Weather requires NO secrets or API keys.
# It runs entirely client-side using the free, key-less Open-Meteo API.
# There is nothing to configure here — this file exists only to document that.
#
# If you add a paid/private API later, define its variable here (placeholder only),
# reference it via import.meta.env.VITE_XXXX, and NEVER commit the real .env file.
